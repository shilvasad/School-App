{
  "name": "school-app",
  "version": "1.0.0",
  "description": "I will build a school app with features that needs in the school I am working now.",
  "main": "index.js",
  "scripts": {
    "install:all": "concurrently \"cd client && npm install\" \"cd api && npm install\"",
    "dev:all": "concurrently \"cd client && npm run dev\" \"cd api && npm run dev\"",
    "dev:client": "cd client && npm run dev",
    "dev:api": "cd api && npm run dev",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "module",
  "devDependencies": {
    "concurrently": "^9.2.0"
  },
  "dependencies": {
    "bcryptjs": "^3.0.2",
    "cors": "^2.8.5",
    "dotenv": "^17.2.1",
    "express": "^5.1.0",
    "jsonwebtoken": "^9.0.2",
    "mongoose": "^8.17.1"
  }
}
